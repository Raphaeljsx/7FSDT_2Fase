# Configuração do Banco de Dados
# Opção 1: DATABASE_URL (formato usado pelo Render, Heroku, etc)
# Exemplo: postgresql://user:password@host:port/database
DATABASE_URL=

# Opção 2: Variáveis individuais (alternativa)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Configuração da Aplicação
PORT=3000
NODE_ENV=production

# Nota: O Render fornece automaticamente a variável DATABASE_URL
# quando você conecta um banco PostgreSQL ao serviço web.
# Você pode usar DATABASE_URL ou as variáveis individuais.
